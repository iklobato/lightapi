apiVersion: v1
kind: Service
metadata:
  name: {{ include "lightapi.fullname" . }}
  labels:
    {{- include "lightapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lightapi.selectorLabels" . | nindent 4 }}
