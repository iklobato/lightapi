[pytest]
asyncio_mode = auto
markers =
    integration: needs a real Postgres/Redis, skipped unless LIGHTAPI_TEST_PG_SYNC etc. are set
env =
    LIGHTAPI_JWT_SECRET=test_secret_key_for_testing
    LIGHTAPI_ENV=test 